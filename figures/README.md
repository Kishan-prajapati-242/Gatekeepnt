# Figures

Paper figures and the script that generates them.

Run `generate_figures.py` to regenerate all figures. Requires matplotlib and numpy.

`eda/` contains exploratory data analysis plots produced by `data/eda.ipynb`.
